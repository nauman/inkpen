{
  "name": "inkpen",
  "version": "0.7.2",
  "description": "TipTap-based rich text editor for Rails. Internal dev tooling only; the gem itself is consumed via the Rails asset pipeline.",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
